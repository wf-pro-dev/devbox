version: "2"
sql:
  - engine: "sqlite"
    schema: "internal/storage/schema.sql"
    queries: "db/queries"
    gen:
      go:
        package: "db"
        out: "internal/db"
        emit_json_tags: true
        emit_empty_slices: true
        emit_pointers_for_null_types: true